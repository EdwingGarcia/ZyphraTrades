<Window x:Class="ZyphraTrades.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ZyphraTrades" Height="560" Width="980"
        WindowStartupLocation="CenterScreen"
        Background="#0F1115"
        Foreground="#E7EAF0">

    <Window.Resources>
        <!-- Tipografía -->
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>

        <!-- Botón moderno -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#2A2F3A"/>
            <Setter Property="Background" Value="#1B202A"/>
            <Setter Property="Foreground" Value="#E7EAF0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border CornerRadius="12"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#232A36"/>
                                <Setter Property="BorderBrush" Value="#3A4354"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#141821"/>
                                <Setter Property="BorderBrush" Value="#4B5670"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#141821"/>
            <Setter Property="BorderBrush" Value="#242B38"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- DataGrid moderno -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#141821"/>
            <Setter Property="Foreground" Value="#E7EAF0"/>
            <Setter Property="BorderBrush" Value="#242B38"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#141821"/>
            <Setter Property="AlternatingRowBackground" Value="#121622"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="ColumnHeaderHeight" Value="44"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#101420"/>
            <Setter Property="Foreground" Value="#B9C2D3"/>
            <Setter Property="BorderBrush" Value="#242B38"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,0"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{Binding RelativeSource={RelativeSource Self}, Path=Background}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1A2030"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#202A3E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Text="ZyphraTrades" FontSize="22" FontWeight="Bold"/>
                <TextBlock Text="Journal &amp; Analytics · SQLite local · Clean Architecture"
                           Opacity="0.70" FontSize="12" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource PrimaryButton}" Margin="0,0,10,0"
                        Content="↻ Refresh" Command="{Binding RefreshCommand}"/>
                <Button Style="{StaticResource PrimaryButton}"
                        Content="+ Add Dummy Trade" Command="{Binding AddDummyTradeCommand}"/>
            </StackPanel>
        </Grid>

        <!-- KPI cards -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Total trades" Opacity="0.70" FontSize="12"/>
                    <TextBlock Text="{Binding Trades.Count}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Symbol focus" Opacity="0.70" FontSize="12"/>
                    <TextBlock Text="AUDUSD" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="4" Style="{StaticResource Card}">
                <StackPanel>
                    <TextBlock Text="Next" Opacity="0.70" FontSize="12"/>
                    <TextBlock Text="Analytics" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Table -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel Margin="16,14,16,10">
                    <TextBlock Text="Trades" FontSize="16" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                    <TextBlock Text="Tip: usa + Add Dummy Trade para poblar datos"
                               Opacity="0.6" FontSize="11" DockPanel.Dock="Right"/>
                </DockPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Trades}" Margin="16,0,16,16">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Open Time (UTC)" Binding="{Binding OpenTime}" Width="180"/>
                        <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="90"/>
                        <DataGridTextColumn Header="Entry" Binding="{Binding Entry}" Width="100"/>
                        <DataGridTextColumn Header="Exit" Binding="{Binding Exit}" Width="100"/>
                        <DataGridTextColumn Header="PnL" Binding="{Binding PnL}" Width="100"/>
                        <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
